# <Название фичи> — как включить и как этим пользоваться (ШАБЛОН)

<!--
Как заполнять:
- Жанр: «включение готовой фичи». Это НЕ технический рунбук выката (он в prod-deploy-log.md), а практическая инструкция ДЛЯ ВЛАДЕЛЬЦА: как включить и как объяснить пользователям.
- Скопируй файл под имя фичи (например `knowledge-access-howto.md`) и заполни.
- Часть A — владельцу по шагам (выкатить → нейтрализовать побочку → заполнить параметр → включить → откат).
- Часть B — готовый текст для конечных пользователей простым языком.
- «Чего пока НЕТ» — честно перечисли ограничения, чтобы не было ложных ожиданий.
- Замени плейсхолдеры {{RUNTIME_EXEC}} и подставь ссылку на технический шаг в prod-deploy-log.md.
-->

> Дата: YYYY-MM-DD. Фича `<slug>` РЕАЛИЗОВАНА в коде (ТЗ `plans/tz/…`).
> Технический рунбук выката — `docs/operations/prod-deploy-log.md`. Этот файл — практическая инструкция для владельца.

## Что важно понять перед включением

<!-- Как заполнять: 2-4 абзаца сути. Главное — предупредить о «подводном камне»: побочном эффекте включения, который надо нейтрализовать (см. Шаг 2 ниже). -->

⚠️ **Подводный камень:** <опиши побочный эффект включения и что с ним делать>.

---

## Часть A. Что сделать владельцу, чтобы включить (по шагам)

### Шаг 1. Выкатить код (один раз)
```bash
{{RUNTIME_EXEC}} <deploy/apply-script>
```
После этого <что появится>. Флаг ещё выключен — **ничего не меняется**.

### Шаг 2. Нейтрализовать побочку
<!-- Как заполнять: если включение фичи имеет побочный эффект (например вторая ось ограничений начнёт прятать данные), опиши как его снять. Дай развилку «если X — ничего не делай / если Y — сделай Z». -->

### Шаг 3. Заполнить параметр владельца
<!-- Как заполнять: бизнес-параметр, без которого фича бессмысленна или опасна (матрица доступа, сумма лимита, состав группы). -->

### Шаг 4. Включить
```bash
<команда включения флага / установки параметра>
{{RUNTIME_EXEC}} <restart>
```
Готово. Теперь работает: <что именно>.

### Шаг 5. 🔴 Откат (мгновенно, если что-то не так)
```bash
<команда возврата флага в исходное>
{{RUNTIME_EXEC}} <restart>
```
Всё возвращается к прежнему поведению, данные не теряются.

---

## Часть B. Короткая инструкция для пользователей (простым языком)

<!-- Как заполнять: готовый текст, который техспециалист даёт сотрудникам. Без жаргона, только «что увидишь и что делать». -->

**Как работает <фича>**

- <пункт 1 — поведение по умолчанию>
- <пункт 2 — что и как меняет пользователь>
- <пункт 3 — что это даёт>

**Коротко:** <одна фраза-резюме>.

---

## Чего пока НЕТ (если понадобится — отдельная доработка)

<!-- Как заполнять: честный список ограничений текущей реализации, чтобы не было ложных ожиданий. -->

- <ограничение 1>
- <ограничение 2>
