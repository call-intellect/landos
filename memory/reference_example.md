<!-- TEMPLATE — заменить перед использованием. Образец типа `reference`: указатель на ресурс/доступ (без секретов). -->
---
name: example-reference-resource
description: "TEMPLATE — где лежат креды/доступы к тестовому окружению: в gitignored-файле, не в чат и не в git."
metadata:
  node_type: memory
  type: reference
  originSessionId: PLACEHOLDER-SESSION-ID
---

TEMPLATE — замени на реальный указатель ресурса (без самих секретов).

**Why:** к этому ресурсу (тестовый аккаунт, доступ к среде, справочные координаты) агент возвращается регулярно, а искать «где это» каждый раз дорого. Сами секреты в память класть нельзя — только указатель, где они лежат.

**How to apply:** креды/токены читай из gitignored-файла окружения в момент использования и подавай прямо в инструмент; никогда не печатай их в чат и не коммить. Здесь храни только путь-указатель и правило обращения, не значения.

Связано: [[example-owner-preference]].
