<!-- TEMPLATE — заменить перед использованием. Образец типа `project`: технический факт/ловушка проекта. -->
---
name: example-project-fact
description: "TEMPLATE — локальный тулчейн: dev-БД поднята, команды сборки идут через {{PKG_MANAGER}}, ключи окружения читаются из локального файла."
metadata:
  node_type: memory
  type: project
  originSessionId: PLACEHOLDER-SESSION-ID
---

TEMPLATE — замени на реальный технический факт проекта {{PROJECT_NAME}}.

**Why:** в ходе задачи выяснилось неочевидное свойство окружения (напр.: локальная БД слушает нестандартный порт; `{{TYPECHECK_CMD}}` зелёный только при заданном флаге; конфиг окружения указывает на локальный, не прод-хост). Установление этого факта стоило времени — фиксируем, чтобы не переоткрывать.

**How to apply:** перед соответствующими действиями опирайся на этот факт — используй `{{INSTALL_CMD}}` / `{{DEV_CMD}}` / `{{TEST_CMD}}` как задано здесь, проверяй, что окружение указывает куда ожидается, не запускай тяжёлые команды вслепую.

Связано: [[example-reference-resource]].
