<!-- Как заполнять: скопируй в architecture/YYYY-MM-DD-<slug>.md. Это ЯДРО ГЕЙТА воронки.
     Документ пишется ЧЕЛОВЕЧЕСКИМ ЯЗЫКОМ для владельца — без имён сервисов/таблиц/эндпоинтов,
     без код-жаргона. Задача: показать «как будет выглядеть», чтобы владелец сказал «да, именно
     это» ДО того, как написан контракт (tz) и тем более код.
     ПРОТОКОЛ approved: агент НИКОГДА не ставит status:approved сам. Ставит только владелец —
     после того как прочитал и явно сказал «да / одобряю / согласен». Пока status:draft — стадия
     tz заблокирована (tz-author отказывается писать ТЗ без approved-архитектуры).
     Frontmatter: type=architecture; status=draft→approved (только владелец); feature=<slug>
     (тот же, что в analysis/tz); owner=кто одобряет; approved_at=дата одобрения (заполняет владелец);
     relates_to=ссылки на analysis и будущее tz. -->
---
type: architecture
status: draft
feature: НАЗВАНИЕ_ФИЧИ_SLUG
date: YYYY-MM-DD
owner: владелец
approved_at:
relates_to:
  - plans/analysis/YYYY-MM-DD-НАЗВАНИЕ_ФИЧИ_SLUG.md
  - plans/tz/YYYY-MM-DD-НАЗВАНИЕ_ФИЧИ_SLUG.md
---

> Что это: _одно предложение — что строим_. Для кого: _роль человеческими словами_.
> Цепочка: анализ → **эта архитектура** → ТЗ. Статус согласования: ожидает одобрения владельца.

# Архитектура «НАЗВАНИЕ_ФИЧИ» (человеческим языком)

## 1. Что это и зачем

_Суть в 1–2 предложениях + кто страдает сегодня и как. Роли — человеческими словами._

## 2. Как сегодня (от чего отталкиваемся)

_Текущее поведение простым языком. Без имён сервисов/таблиц/эндпоинтов._

## 3. Что делаем (решение простыми словами)

_Суть решения. Честно: расширяем существующее или строим новое._

## 4. Как это будет выглядеть

### Сценарии «было → стало»

**Сценарий: _название пути_**
- Было: _что делает_ → _что видит_ → _чем недоволен_.
- Стало: _что делает_ → _что видит_ → _какой исход_.

### Макет экрана (ASCII)

```
┌──────────────────────────────┐
│  _грубая текстовая рамка_     │
│  [ кнопка ]   поле: ______    │
└──────────────────────────────┘
```

### Живой образец

_Конкретный заполненный пример того, что реально увидит пользователь (текст, значения, данные), а не абстракция._

## 5. Делаем поверх существующего (масштаб)

_Что переиспользуем (функциональными именами), что добавляем. Честная оценка масштаба._

## 6. Границы — что НЕ делаем

- _Что осознанно за рамками + причина + «на потом», если знаем._

## 7. Дорешал сам

Продуктовые развилки, закрытые тобой (на основе анализа/кода/рекомендации). Вопросов владельцу не было — строка «владельцу вопросов не задавал, потому что …» вместо таблицы.

| # | Решил | На чём стою |
|---|---|---|
| Р1 | | |
| Р2 | | |

## 8. Что реши ты (открытые развилки)

> **Развилка 1.** Рекомендую X, потому что Y. Не так — поправь.
> **Развилка 2.** …

## 9. Как поймём, что получилось (критерии приёмки)

- _Критерий «сделано» глазами пользователя, не машинные предикаты._
- _…_

## 10. Связи

- Анализ: `plans/analysis/YYYY-MM-DD-НАЗВАНИЕ_ФИЧИ_SLUG.md`
- Дальше → ТЗ: `plans/tz/YYYY-MM-DD-НАЗВАНИЕ_ФИЧИ_SLUG.md` (пишется ПОСЛЕ `status: approved`)

---

<!-- ПРОТОКОЛ ОДОБРЕНИЯ (не удаляй):
  1. Агент готовит документ, оставляет status: draft.
  2. Владелец читает раздел 4 («как будет выглядеть») и разделы 7–9.
  3. Владелец говорит «да / одобряю» → ВЛАДЕЛЕЦ (или агент с его явного разрешения) ставит
     status: approved и проставляет approved_at.
  4. Только после этого открывается стадия tz. Агент сам approved не ставит. -->
