---
name: <slug-kebab-case>
title: <человеческое название процесса, например: «Пост-обработка входящего события»>
trigger_type: event | cron | user_action | webhook
status_overall: implemented | partial | designed_only
last_audited: YYYY-MM-DD
owners_human:
  - <роль или имя ответственного, например: «продакт-менеджер модуля X»>
related_plans:
  - plans/tz/...
  - plans/analysis/...
related_projects:
  - 01_projects/<feature>.md
---

# <Название процесса>

> **Как читать этот файл:** разделы 1–4 — для не-программиста (что происходит и зачем). Раздел 5 — для разработчика (где живёт код). Раздел 8 — список расхождений с ТЗ. Шаги между разделами 3 и 5 пронумерованы синхронно.

## 1. О чём это (бытовой рассказ)

2–4 абзаца чистым русский, без технических терминов. Что происходит, зачем это нужно, для кого результат. Если технический термин обязателен — пояснение в скобках простым языком.

Идея: человек, который никогда не открывал код {{PROJECT_NAME}}, должен прочитать этот раздел и понять, **что именно делает система** в рамках этого процесса.

## 2. Что запускает (триггер)

- **Тип триггера:** событие пользователя / завершение другого процесса / расписание / внешний вебхук.
- **Кто или что инициирует:** опиши одной строкой бытовым языком.
- **Технический источник сигнала** (одна строка для перехода в раздел 5): например, `webhook POST /<эндпоинт>` или `cron <cron-выражение>`.

## 3. Шаги процесса (общий список)

Нумерованный список шагов в одном предложении каждый. Бытовой смысл, без технических деталей.

1. <шаг 1: что происходит первым>
2. <шаг 2>
3. <шаг 3>
4. <шаг 4>
5. <шаг 5>

## 4. Что получается на выходе

- **Кому:** инициатору / всем участникам / ответственному / в общее хранилище.
- **В каком виде:** запись в БД / уведомление / страница в интерфейсе / поле в карточке.
- **Где это видно:** конкретный путь страницы (например, `/<раздел>/[id]`).

## 5. Технический разрез (по шагам)

> Номера шагов синхронизированы с разделом 3.

| # | Шаг (бытовой) | Что делает технически | Где живёт код | Очередь / cron / эндпоинт | Записывает в БД | Статус |
|---|---|---|---|---|---|---|
| 1 | <шаг> | <тех. действие> | `<путь к модулю>` | `<очередь-cron>` / `<cron-выражение>` / `POST /<эндпоинт>` | `<таблица БД A>`, `<таблица БД B>` | ✅ |
| 2 | <шаг> | <тех. действие> | `<путь к модулю>` | `<очередь-cron>` | `<таблица БД C>` | ⚠️ частично — <что не так> |
| 3 | <шаг> | <тех. действие> | `<путь во фронтенде>` | `GET /<эндпоинт>` | — | ✅ |
| 4 | <шаг> | <тех. действие> | `<путь к модулю>` | `<очередь-cron>` | `<таблица БД D>` | ❌ только в ТЗ |
| 5 | <шаг> | <тех. действие> | `<путь к модулю>` | `<очередь-cron>` | `<таблица БД E>` | ✅ |

**Легенда статусов:**
- ✅ реализован — код есть, работает в проде или dev-окружении.
- ⚠️ частично — код есть, но не покрывает все случаи / есть TODO / отключён по умолчанию.
- ❌ только в ТЗ — описан в `plans/tz/...`, кода нет.
- 🗑 отменён — был, но удалён или признан ненужным.

### 5.1 Структуры данных, через которые проходит процесс

- `<СущностьA>` → `<СущностьB>` → `<СущностьC>` (если применимо).
- Какие поля заполняются на каком шаге.

### 5.2 LLM-вызовы внутри процесса

| Шаг | taskType | Primary модель | Фолбэк | Где промпт |
|---|---|---|---|---|
| 2 | `<taskType>` | `<модель>` | `<фолбэк>` → `<фолбэк>` | `<путь к промпту>` |
| ... | | | | |

## 6. Точки отказа и наблюдаемость

- **Метрики:** `<metric_name_1>{label=...}`, `<metric_name_2>`.
- **Очереди / cron** (для проверки в панели воркеров): `<очередь-cron>`, `<очередь-cron>`.
- **Логи:** имя логгера / контекстное поле для трассировки.
- **Известные грабли** (ссылки на [[../02_architecture/code-pitfalls]]): описание + ссылка на конкретный пункт.
- **Ручные кнопки** (где можно пнуть / посмотреть): `<путь в интерфейсе>`.

## 7. Связанные процессы

- [[other-process-1]] — как связан (например, «является триггером для»).
- [[other-process-2]] — как связан.

## 8. Расхождения «задумано vs реализовано»

- **Заложено в ТЗ, но не реализовано:** `<что>` — ссылка на ТЗ и причина (отложено / не критично / зависит от другой фазы).
- **Реализовано иначе:** `<что>` — почему пришлось отступить, ссылка на коммит/рефлексию.
- **Реализовано, но не описано в ТЗ:** `<что>` — нужно добавить в `01_projects/...` или это анти-паттерн.

## 9. История изменений процесса

| Дата | Что изменилось | Коммит/рефлексия |
|---|---|---|
| YYYY-MM-DD | Создан | <commit> |
| | | |
