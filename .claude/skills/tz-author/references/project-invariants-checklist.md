# Чек-лист инвариантов проекта + «куда что писать» — СКЕЛЕТ

> Это **рыба под заполнение**. Категории ниже универсальны и переносимы; конкретные правила под них
> ты (или владелец проекта) вписываешь один раз под свой стек и домен. Прогоняй заполненный чек-лист
> перед выдачей каждого ТЗ. **Не дублируй текст правил в тело ТЗ — ссылайся** на скилл/файл проекта,
> но проверь, что фича их не нарушает. Источник правды о фактическом состоянии — `second-brain/`.
>
> Как заполнять: под каждой категорией замени `TODO(проект): …` на конкретный инвариант с ссылкой на
> скилл проекта и на память/feedback. Неприменимую категорию помечай `[N/A: почему]`, не удаляй.

## Стек и рантайм
- [ ] TODO(проект): допустимые языки/рантаймы продакшен-пути ({{PROJECT_STACK}}); что делать с чужеродным рантаймом (порт отдельным ТЗ vs исключения). Скилл: `core-engineering-standards`.
- [ ] Команды проверки — канонические: `{{TYPECHECK_CMD}}` / `{{LINT_CMD}}` / `{{BUILD_CMD}}` / `{{TEST_CMD}}`, пакетный менеджер `{{PKG_MANAGER}}` (никаких чужих).

## БД / миграции
- [ ] TODO(проект): дисциплина изменения схемы (миграции vs push, где источник правды).
- [ ] TODO(проект): спец-индексы/расширения — где живут (не в основной схеме, если так принято).
- [ ] TODO(проект): правила для скриптов работы с БД (как создаётся клиент, откуда импорты, идемпотентность).

## Config: ENV vs runtime-крутилки
- [ ] TODO(проект): что законно в ENV (секреты, строки подключения, bootstrap) и как читается (типизованная конфиг-схема; никаких сырых обращений к окружению в коде).
- [ ] TODO(проект): что уходит в runtime-настройки (пороги/лимиты/флаги/retention/веса → админка/сторедж с code-fallback, не ENV и не хардкод).
- [ ] TODO(проект): активные снимки (зафиксированные цены/условия) ретроактивно не пересчитывать.

## Внешние API / провайдеры
- [ ] TODO(проект): переключаемые endpoint'ы/креды — через конфиг, не хардкод.
- [ ] TODO(проект): для LLM-фич — стандарт выбора провайдера/модели; раздел «Совместимость с prompt caching» (стабильный SYSTEM, переменные данные в конце user) или явное «Не релевантно»; политика self-improving/human-gate.

## Доступ / tenancy
- [ ] TODO(проект): как достаётся tenantId и где обязателен; индексы `([tenantId, …])`.
- [ ] TODO(проект): модель RBAC; что привилегированная роль НЕ имеет права видеть.

## Контракты back/front
- [ ] TODO(проект): каждый эндпоинт — типизованный DTO + описание API + machine-readable коды ошибок.
- [ ] TODO(проект): фронт по слоистой модели проекта (напр. ApiDto→DomainModel→UiModel), единый клиент API.
- [ ] TODO(проект): фоновые воркеры/cron — где живут; единый источник правды для перечней фронт↔бэк (accept-форматы, enum).

## Флаги / rollout
- [ ] TODO(проект): политика выката (готовая фича — включённой? наблюдение-потом-включим запрещено?).
- [ ] TODO(проект): допустимые виды флага (аварийный рубильник / «решение владельца с параметром»); флаг «на всякий случай» не вводить.
- [ ] TODO(проект): любой флаг — строкой в реестре флагов проекта.

## Идемпотентность
- [ ] TODO(проект): seed/patch/backfill/migrate — повторный прогон = no-op (acceptance-критерий); регистрация в едином агрегаторе деплоя.

## UI и копирайтинг
- [ ] TODO(проект): язык UI, токены оформления, запрещённые паттерны стилей.
- [ ] TODO(проект): правила probe/уточняющих диалогов внутри продукта.

## Приоритеты сейчас (deprioritized / не поднимать)
- [ ] TODO(проект): темы, которые сейчас НЕ приоритет и не должны всплывать в ТЗ самовольно.

## Куда писать (для секции DoD / deploy-log) — СКЕЛЕТ
> Заполни маппинг «что изменил → какую заметку в `second-brain/` обновить → какой шаг деплой-лога затронут».

| Что изменил | Куда (second-brain) | deploy-log шаг |
|---|---|---|
| Новая колонка/таблица | TODO(проект): data-model заметка | TODO(проект): шаг схемы |
| Новый модуль/контроллер | TODO(проект): module-map заметка | — |
| Новый воркер/job/агент | TODO(проект): jobs/queues заметка | TODO(проект): smoke-шаг |
| Новый API-эндпоинт | TODO(проект): api-layer заметка | TODO(проект): API smoke |
| Спец-индексы/расширения БД | — | TODO(проект): шаг индексов |
| patch-/seed-/backfill-/migrate-/setup-скрипт | TODO(проект): профильная заметка | TODO(проект): шаги скриптов |
| Новая ENV в конфиг-схеме | — | TODO(проект): шаг ENV |
| Новый флаг | TODO(проект): реестр флагов | TODO(проект): шаг ENV/флагов |
